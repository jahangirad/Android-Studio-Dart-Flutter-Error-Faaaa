kotlin.code.style=official

# Increase the heap size for the Gradle Daemon to prevent OutOfMemoryError.
# 2GB is a recommended starting point for IntelliJ Platform plugin development.
org.gradle.jvmargs=-Xmx2g

# Enable Gradle's build cache to speed up subsequent builds.
org.gradle.caching=true

kotlin.stdlib.default.dependency=false
